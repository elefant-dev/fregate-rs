[package]
name = "resources"
version = "0.1.0"
edition = "2021"

[dependencies]
hyper = "0.14.*"
prost = "0.11.*"
tonic = "0.9.*"

[build-dependencies]
tonic-build = { version = "0.9.*", features = ["prost"] }
